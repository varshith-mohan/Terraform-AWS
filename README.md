# Terraform-AWS
Terraform is an IaC software tool that provides a consistent command line interface (CLI) workflow to manage hundreds of cloud services, and allowing users to define, provision, and update cloud and on-premises resources like virtual machines and networks using human-readable configuration files
